{
  "name": "key-derivation-reference",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "gen": "tsc --noEmit && node --no-warnings --loader ts-node/esm ./src/cli.ts gen --dir ../test-vectors",
    "test": "tsc --noEmit && vitest run --coverage.enabled=true --coverage.exclude=src/cli.ts --reporter verbose",
    "find-seeds-esk": "tsc --noEmit && node --no-warnings --loader ts-node/esm ./src/cli.ts find-seeds --key shield-esk",
    "find-seeds-dust": "tsc --noEmit && node --no-warnings --loader ts-node/esm ./src/cli.ts find-seeds --key dust-pk --number 3",
    "format": "prettier --write --config ../../../.prettierrc.json ."
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@midnight-ntwrk/ledger-v7": "^7.0.2",
    "@noble/curves": "^1.8.1",
    "@scure/base": "^1.1.9",
    "@subsquid/scale-codec": "^4.0.0",
    "@types/node": "^22.0.0",
    "commander": "^14.0.0"
  }
}
